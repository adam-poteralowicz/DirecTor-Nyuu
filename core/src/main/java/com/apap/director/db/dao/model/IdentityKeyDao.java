package com.apap.director.db.dao.model;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "IDENTITY_KEY".
*/
public class IdentityKeyDao extends AbstractDao<IdentityKey, Long> {

    public static final String TABLENAME = "IDENTITY_KEY";

    /**
     * Properties of entity IdentityKey.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property IdentityKeyId = new Property(1, Integer.class, "identityKeyId", false, "IDENTITY_KEY_ID");
        public final static Property Name = new Property(2, String.class, "name", false, "NAME");
        public final static Property Serialized = new Property(3, byte[].class, "serialized", false, "SERIALIZED");
    }


    public IdentityKeyDao(DaoConfig config) {
        super(config);
    }
    
    public IdentityKeyDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"IDENTITY_KEY\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: id
                "\"IDENTITY_KEY_ID\" INTEGER," + // 1: identityKeyId
                "\"NAME\" TEXT," + // 2: name
                "\"SERIALIZED\" BLOB);"); // 3: serialized
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"IDENTITY_KEY\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, IdentityKey entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        Integer identityKeyId = entity.getIdentityKeyId();
        if (identityKeyId != null) {
            stmt.bindLong(2, identityKeyId);
        }
 
        String name = entity.getName();
        if (name != null) {
            stmt.bindString(3, name);
        }
 
        byte[] serialized = entity.getSerialized();
        if (serialized != null) {
            stmt.bindBlob(4, serialized);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, IdentityKey entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        Integer identityKeyId = entity.getIdentityKeyId();
        if (identityKeyId != null) {
            stmt.bindLong(2, identityKeyId);
        }
 
        String name = entity.getName();
        if (name != null) {
            stmt.bindString(3, name);
        }
 
        byte[] serialized = entity.getSerialized();
        if (serialized != null) {
            stmt.bindBlob(4, serialized);
        }
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public IdentityKey readEntity(Cursor cursor, int offset) {
        IdentityKey entity = new IdentityKey( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getInt(offset + 1), // identityKeyId
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2), // name
            cursor.isNull(offset + 3) ? null : cursor.getBlob(offset + 3) // serialized
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, IdentityKey entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setIdentityKeyId(cursor.isNull(offset + 1) ? null : cursor.getInt(offset + 1));
        entity.setName(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
        entity.setSerialized(cursor.isNull(offset + 3) ? null : cursor.getBlob(offset + 3));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(IdentityKey entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(IdentityKey entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(IdentityKey entity) {
        return entity.getId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
